# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (SERVER-ONLY — never prefix with NEXT_PUBLIC_)
# Required for production: enables tightened RLS that blocks direct DB abuse.
# Find this in Supabase Dashboard > Settings > API > service_role key.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Secret for protecting the /api/migrate endpoint (use any random string)
CRON_SECRET=your_cron_secret_here

# Base URL (for migration API calls)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
